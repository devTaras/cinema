@model Microsoft.AspNetCore.Identity.IdentityUser

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Actor</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt class = "col-sm-2">
            Roles
        </dt>
        <dd class = "col-sm-10">
            <form asp-action="Save" method="post">
                <input type="hidden" name="userId" value="@Model.Id"/>
                <label for="roles" class="control-label"></label>
                <select name="roles" multiple="multiple" id="roles" class="form-control" asp-items="ViewBag.Roles"></select>
                <input type="submit" />
            </form>
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
